Please do this these steps by step and first think create task and todos and then complete the task systematically. at about me dashboard admin page:

- So at aboutme admin dashboard awards tab the edit page the dates ui input is not having placeholder preview data and if its a new award then its will use current date as the default value
- Then at Story tab there must only be a single item and there should be edit button to edit that single item not a list view
- Then at Impact tab there should be a list view to add remove and reorder multiple impact items and defautl values of that fronted page should be prefilled in and there should create and edit button for each item
- At Travel tab its only one sections view so there shouldn't be any order or Additional json data feild
- At Corporate tab its only one sections view the corporate journey items are not editable so please make them editable with create and edit button for each item and prefill the default values of that frontend page in the dashboard form inputs.
- At Associate tab its only one sections view the associate items are not editable so please make them editable with create and edit button for each item and prefill the default values of that frontend page in the dashboard form inputs.
