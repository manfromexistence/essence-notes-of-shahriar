# Docs

This folder contains documentation files for the project.

```markdown

Please do this these steps by step and first think create task and todos and then complete the task systematically.

- Current the events at events create and update page at dashboard events has to have a date but its empty for some reason and everytime I have to manually add the date again. Fix this issue so that the date is saved properly when creating or updating events and if nothing is changed then its should stay the same and if creating events then it will use current date

```
